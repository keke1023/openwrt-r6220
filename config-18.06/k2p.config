# Phicomm K2P（ramips/mt7621）—— 仅用于 openwrt-18.06-k5.4 分支测试
#
# 分支隔离说明：
#   - 本文件放在独立的 config-18.06/ 目录，与 23.05 的 config/ 互不污染；
#     build.yml 的 prepare 会按所选 source_branch 自动选 config 或 config-18.06。
#   - 18.06 与 23.05 的 DEVICE 符号 / feeds 差异很大，故 18.06 机型不引入
#     luci-app-ssr-plus / ngrokc / frpc 等依赖 helloworld(master) 的插件，
#     因为 master 分支的 helloworld 在 18.06 上不兼容（见 scripts/diy-part1.sh 的分支隔离逻辑）。
#   - 默认管理地址 192.168.6.1 由 scripts/diy-part2.sh 统一设置（sed 替换 192.168.1.1）。

CONFIG_TARGET_ramips=y
CONFIG_TARGET_ramips_mt7621=y
CONFIG_TARGET_ramips_mt7621_DEVICE_phicomm_k2p=y

# LuCI 与 Web 管理
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-ssl-openssl=y
CONFIG_PACKAGE_libustream-mbedtls=n

# CCACHE（workflow 也会再追加一次，这里写上也无害）
CONFIG_CCACHE=y
